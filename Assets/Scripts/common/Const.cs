using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum Difficulty
{
    Medium = 0,
    Hard = 1,
    VeryHard = 2,
    Impossible = 3,
    LoadGame = 4,
}

public enum Gravity
{
    Down = 0,
    Up = 1,
}